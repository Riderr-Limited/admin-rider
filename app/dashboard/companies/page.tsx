import React from "react";

const page = () => {
  return (
    <div>
      here we should have the compainies registerd with us, there should be
      something like a table which shows all the companies a tab to switch
      beteen, pending, approved and rejected companies
    </div>
  );
};

export default page;
