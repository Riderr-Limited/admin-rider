import React from "react";

const page = () => {
  return (
    <div>
      here is the dashboard page we should have some stats here pending on
      requests and some other stuff also add sidebar, which links to other pages
      add navbar too, and it should have notifications, when click on the it
      should open a notification panel
    </div>
  );
};

export default page;
